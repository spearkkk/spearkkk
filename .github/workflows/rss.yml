name: Update RSS in README.md

on:
  schedule:
    - cron: 0 8 * * *

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: JasonEtco/rss-to-readme@v1
        with:
          feed-url: https://spearkkk.dev/rss
          readme-section: feed
